@page
@model Tippy.Pages.NodeLog.IndexModel
@{
    ViewData["Title"] = "Logs";
}

@if (Model.ActiveProject != null)
{
    <div class="level">
        <div class="level-left">
            <h1 class="title">Logs</h1>

        </div>
        <div class="level-right">
            <form method="post">
                <button asp-page="./OpenFolder" class="button is-success is-small">
                    <span class="icon">
                        <i class="far fa-folder-open"></i>
                    </span>
                    <span>Open log folder</span>
                </button>
            </form>

        </div>
    </div>

    <input type="hidden" id="project-id" value="@Model.ActiveProject.Id" />
    <div class="log-box" id="log-box"></div>

    @section Scripts {
        @{ await Html.RenderPartialAsync("_SignalrScriptsPartial"); }
    }
}
else
{
    <div class="notification is-warning">
        <p class="ml-3">
            <span class="icon has-text-danger">
                <i class="fas fa-exclamation-triangle fa-3x"></i>
            </span>
        </p>
        <p>Current project/chain is not running. Please <a asp-page="/Home/Index">start</a> and select a project first.</p>
    </div>
}
