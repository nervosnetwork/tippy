@page
@model Tippy.Pages.Home.IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<h1 class="title">Dashboard</h1>

@if (TempData["Message"]?.ToString().Length > 0)
{
    <div class="notification is-success">
        <button class="delete"></button>
        <div>@TempData["Message"]</div>
    </div>
}

@if (Model.Projects.Count == 0)
{
    <div class="notification is-warning">
        <p class="ml-3">
            <span class="icon has-text-danger">
                <i class="fas fa-mug-hot fa-3x"></i>
            </span>
        </p>
        Create your first project and launch a CKB devnet quickly.
    </div>
    <form asp-page="./QuickStart" method="post">
        <button class="button is-primary">
            <span class="icon">
                <i class="fas fa-magic"></i>
            </span>
            <span>Launch devnet</span>
        </button>
    </form>
}

@if (Model.ActiveProject != null)
{
    <h2 class="subtitle">@Model.ActiveProject.Name</h2>
}
